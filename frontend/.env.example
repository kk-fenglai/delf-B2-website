# Dev: leave empty — Vite proxies /api to localhost:4000.
# Prod build defaults to direct Fly API (skips Vercel /api proxy).
# Override when using a custom API host, e.g. https://api.exampass.website
# VITE_API_ORIGIN=https://delf-b2-website.fly.dev
